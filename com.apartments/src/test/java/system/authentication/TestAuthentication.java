package system.authentication;

import apartments_page_library.HomePage;
import base.BasePage;
import org.testng.Assert;
import org.testng.annotations.Test;

public class TestAuthentication extends BasePage {

//    @Test(dataProviderClass = data_providers.DataProviders.class, dataProvider = "testAuthentication")
//    public void testAuthentication(String emailAddress, String password, String text) {
//        HomePage homePage = new HomePage();
//        homePage.doSignIn(emailAddress, password);
//        Assert.assertEquals(homePage.getAccountNameText(), text);
//    }
}
